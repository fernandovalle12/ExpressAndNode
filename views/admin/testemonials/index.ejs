<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Posts</title>
  <%- include('../../imports'); -%>
</head>
<body>
  <%- include('../../navbar'); -%>
  <div class="container">
    <div class="row">
        <h1>Administração de Testemonials</h1>
    </div>

    <div class="row mb-2">
        <a href="/admin/testemonials/create" class="btn btn-success">Novo Testemonials</a>
    </div>

    <div class="row">
        <table class="table">
            <thead class="thead-dark">
              <tr>
                <th scope="col">Código</th>
                <th scope="col">Título</th>
              </tr>
            </thead>
            <tbody>
              <% testemonials.forEach((testemonial) => { %>
                <tr>
                  <td><%= testemonials.id %></td>
                  <td><%= testemonials.title %></td>
                </tr>
              <% }); %>
              
            </tbody>
          </table>
    </div>
  </div>
</body>
</html>