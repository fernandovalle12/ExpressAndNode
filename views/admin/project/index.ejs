<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Projects</title>
  <%- include('../../imports'); -%>
</head>
<body>
  <%- include('../../navbar'); -%>
  <div class="container">
    <div class="row">
        <h1>Administração de Projects</h1>
    </div>

    <div class="row mb-2">
        <a href="/admin/project/create" class="btn btn-success">Novo Project</a>
    </div>

    <div class="row">
        <table class="table">
            <thead class="thead-dark">
              <tr>
                <th scope="col">Código</th>
                <th scope="col">Título</th>
              </tr>
            </thead>
            <tbody>
              <% project.forEach((project) => { %>
                <tr>
                  <td><%= project.id %></td>
                  <td><%= project.title %></td>
                </tr>
              <% }); %>
              
            </tbody>
          </table>
    </div>
  </div>
</body>
</html>